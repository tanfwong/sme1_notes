---
numbering:
  equation: false
---

# Random Pair

## Joint Distribution
- Let $\mathcal{B}^2$ be the Borel $\sigma$-field in $\mathbb{R}^2$,
  i.e., $\mathcal{B}^2$ is the $\sigma$-field generated by all the
  half-spaces of the form $(-\infty, x) \times(-\infty, y)$ for all
  $x, y \in \mathbb{R}$.
- Starting from the probability space $(\Omega, \mathcal{F}, P)$, the
  mapping $(X,Y): \Omega \rightarrow \mathbb{R}^2$ is called a
  ***random pair (pair of r.v.'s)*** if $(X,Y)^{-1}(S) \in
  \mathcal{F}$ for every $S \in \mathcal{B}^2$, i.e., the mapping is
  measurable.
- Let $P_{X,Y}$ be the probability measure on $\mathcal{B}^2$ defined by
  $$
  P_{X,Y}(S) = P\left( (X,Y)^{-1}(S) \right) = P\left( (X,Y) \in S \right)
  $$
  for every $S \in \mathcal{B}^2$. Then the random pair  $(X,Y)$
  induces a probability space $\left(\mathbb{R}^2, \mathcal{B}^2,
  P_{X,Y} \right)$.

- $P_{X,Y}$ is called the ***joint distribution*** of the random pair
  $(X,Y)$.

- The following results can be proved: 
:::{prf:remark}
1. $(X,Y)$ is a random pair if and only if $X$ and $Y$ are r.v.'s. 

2. $P_{X,Y}(A \times B)=P\left(X^{-1}(A) \cap Y^{-1}(B)\right)$ for
   every $A,B \in \mathcal{B}$. \
   In particular, $P_{X,Y}\left( (-\infty,x] \times  (-\infty,y]
   \right) = P(X\leq x, Y \leq y)$ for all $x,y \in \mathbb{R}$.

:::

## Joint Cumulative Distribution Function
- The ***joint cdf*** of the random pair $(X,Y)$ is defined as
  $$
  F_{X,Y}(x, y) 
  = P_{X,Y}\left((-\infty, x] \times(-\infty, y] \right)
  =P(X \leq x, Y \leq y).
  $$

- As in case of a single r.v., the joint distribution $P_{X,Y}$ is
  completely specified by the joint cdf $F_{X,Y}(x,y)$.

:::{prf:property} Properties of joint cdf
%:::{prf:lemma} Properties of joint cdf
1. $F_{X,Y}(\infty, \infty)=1$, $F_{X,Y}(-\infty,
  y)=F_{X,Y}(x,-\infty)=0$, $F_{X,Y}(x, \infty)=F_{X}(x)$ and
  $F_{X,Y}(\infty, y)=F_{Y}(y)$.

2. If $x_{1} \leq x_{2}$ and $y_{1} \leq y_{2}$, then
   $F_{X,Y}\left(x_{1}, y_{1}\right) \leq F_{X,Y}\left(x_{2},
   y_{2}\right)$.

3. $\lim _{\epsilon \downarrow 0, \delta \downarrow 0}
   F_{X,Y}(x+\epsilon, y+\delta) = F_{X,Y}(x, y)$.

4. Fix $x_{1} < x_{2}$ and $y_{1} < y_{2}$. Then
   $$
   \begin{aligned}
   & \!\! P\left(x_{1}<z \leq x_{2}, y_{1}<Y \leq y_{2}\right) \\
   &= F_{X,Y}\left(x_{2}, y_{2}\right)-F_{X,Y}\left(x_{2},
   y_{1}\right)-F_{X,Y}\left(x_{1}, y_{2}\right)+F_{X,Y}\left(x_{1},
   y_{1}\right).
   \end{aligned}
   $$
   See the figure below for an illustration of this property:
   ```{image} images/FXY.png
   :alt: Sqaure area in terms of joint cdf
   :align: center
   :height: 300px
   ```
:::

- Referring back to Remark 1.1, $F_X(x)$ and $F_Y(y)$ in Property 1.1
  are the cdfs of the random variables $X$ and $Y$, respectively. They
  are often referred to as ***marginal*** cdfs.

## Joint Probability Density Function
- If there is a non-negative function $f_{X,Y}(x, y)$ such that 
  $$
  P\left( (X,Y) \in S\right) = \int_S f_{X,Y}(u,v) dudv
  $$
 for every $S \in \mathcal{B}^2$, then $(X,Y)$ is called a
  ***continuous*** random pair and the non-negative $f_{X,Y}(x, y)$ is
  called the ***joint pdf*** of $(X,Y)$.

:::{prf:remark}
- There is a more general way to extend the concept of absolute
  continuity to this case of a random pair, and it can be shown that
  the condition of absolute continuity is equivalent to the existence
  of the joint probability density function above. The latter result
  is called the Radon-Nikodym theorem. 
- We instead take the approach above to define the joint pdf to
  minimize the amount of math machinery needed here.
:::

:::{prf:property} Properties of joint pdf
%:::{prf:lemma} Properties of joint pdf
1. $F_{X,Y}(x, y)=\int_{-\infty}^{x} \int_{-\infty}^{y} f_{X,Y}(u, v) d
   u d v$.
2. $f_{X,Y}(x, y)=\frac{\partial^{2} F_{X,Y}(x, y)}{\partial x
   \partial y}$ on $\mathbb{R}^2$ except for perhaps a set of area
   zero.

3. $\int_{-\infty}^{\infty} \int_{-\infty}^{\infty} f(x, y) d x d
   y=1$.

4. $f_{X}(x)=\int_{-\infty}^{\infty} f_{X,Y}(x, y) dy$ and
  $f_{Y}(y)=\int_{-\infty}^{\infty} f_{X,Y}(x, y) d x$.
:::
- Referring back to Remark 1.1, $f_X(x)$ and $f_Y(y)$ in Property 2.4
  are the pdfs of the random variables $X$ and $Y$, respectively. They
  are often referred to as ***marginal*** pdfs.

:::{prf:example} Joint continuous random pair
   Consider the joint pdf of $(X,Y)$ as shown in the plot below:
   ```{image} images/jcont.png
   :alt: Joint pdf and cdf for a continuous random pair
   :align: center
   :height: 350px
   ```
   The joint cdf 
   $$
   \begin{aligned}
   F_{X,Y}(x, y) & =\int_{-\infty}^{x} \int_{-\infty}^{y} f_{X,Y}(u, v) d v d u \\
   & = \begin{cases}
   0 & \text{ if  }  x<0 \text{ or } y<0 \\
   2 x y & \text{ if  } x \geq 0, y \geq 0, x+y<1 \\
   2 x y-(x+y-1)^{2} & \text{ if  } 0\leq x<1, 0 \leq y<1, x+y \geq 1 \\
   2 x-x^{2} & \text{ if  } 0 \leq x<1, y \geq1 \\
   2 y-y^{2} & \text{ if  } x \geq 1 , 0 \leq y<1 \\
   1 & \text{ if  } x \geq 1,y \geq 1
   \end{cases}
   \end{aligned}
   $$
   and the marginal cdfs
   $$
   \begin{aligned}
   F_{X}(x) &=F_{X,Y}(x, \infty)=
   \begin{cases}
   0 & \text{ if  } x<0 \\
   2 x-x^{2} & \text{ if  }  0 \leq x<1 \\
   1 & \text{ if  }  x \geq 1
   \end{cases} \\
   F_{Y}(y) &=F_{X,Y}(\infty, y)=
   \begin{cases}
   0 & \text{ if  } y<0 \\
   2 y-y^{2} & \text{ if  }  0 \leq y<1 \\
   1 & \text{ if  }  y \geq 1.
   \end{cases}
   \end{aligned}
   $$
   Thus, the marginal pdfs
   $$
   \begin{aligned}
   f_{X}(x) &=\frac{d F_{X}(x)}{d x}
   =\begin{cases}
   2-2 x &  \text{ if  }  0 \leq x<1 \\
   0 &  \text {otherwise}
   \end{cases} \\
   f_{Y}(y) &=\frac{d F_{Y}(y)}{d y}
   =\begin{cases}
   2-2 y &  \text{ if  }  0 \leq y<1 \\
   0 &  \text {otherwise.}
   \end{cases}
   \end{aligned}
   $$
:::

## Joint Probability Mass Function
- If the range of $(X,Y)$ is discrete, we may define the ***joint
  pmf*** of the random pair be
  $$
  p_{X,Y}\left(x_{i}, y_{k}\right)=P\left(X=x_{i}, Y=y_{k}\right)
  $$
  for all $(x_i, y_j)$ in the ranges of $(X,Y)$.

%:::{prf:lemma} Properties of joint pdf
:::{prf:property} Properties of joint pmf
1. $F_{X,Y}(x,y) = \sum_{x_i \leq x, y_j \leq y} p_{X,Y}(x_i, y_j)$. 

2. $p_{X}\left(x_{i}\right)=\sum_{j} p_{X,Y}\left(x_{i}, y_{j}\right)$
  and $p_{Y}\left(y_{j}\right)=\sum_{i} p_{X,Y}\left(x_{i},
  y_{j}\right)$.
:::
- Referring back to Remark 1.1, $p_X(x)$ and $p_Y(y)$ in Property 3.2
  are the pmfs of the random variables $X$ and $Y$, respectively. They
  are often referred to as ***marginal*** pmfs.


:::{prf:example} Tossing a coin twice with a skillful hand
   $\Omega=\{H H, H T, T H, T T\}$, $\mathcal{F}=2^{\Omega}$, and\
   $P(H H)=0.4$, $P(T T)=0.4$, $P(H T)=0.1$, $P(TH)=0.1$.\
   Define the random pair $(X,Y)$ by
   $$
   \begin{aligned}
   X(HH) &=1, \quad X(HT)=1, \quad X(TT)=0, \quad X(TH)=0\\
   Y(HH) &=1, \quad Y(HT)=0, \quad Y(TT)=0, \quad Y(TH)=1.
   \end{aligned}
   $$
   Referring to the figure below,
   ```{image} images/jdiscrete.png
   :alt: Joint pmf and cdf for a discrete random pair
   :align: center
   :height: 350px
   ```
   it is not hard to see that the joint pmf 
   $$ 
   p_{X,Y}(x, y)= 
   \begin{cases}
   0.4 & \text{ if  } (x, y)=(0,0) \\ 
   0.1 & \text{ if  } (x, y)=(0,1) \\
   0.1 & \text{ if  } (x, y)=(1,0) \\
   0.4 & \text{ if  } (x, y)=(1,1) ,
   \end{cases}
   $$
   and thus the marginal pmfs
   $$
   \begin{aligned}
   p_{X}(x) &= p_{X,Y}(x, 0)+p_{X,Y}(x, 1) 
   = \begin{cases}
   0.5 & \text{ if  }  x=0 \\
   0.5 & \text{ if  }  x=1
   \end{cases} \\
   p_{Y}(y) &= p_{X,Y}(0, y)+p_{X,Y}(1, y) 
   = \begin{cases}
   0.5 & \text{ if  }  y=0 \\
   0.5 & \text{ if  }  y=1.
   \end{cases}
   \end{aligned}
   $$
   Hence, the joint cdf
   $$
   F_{X,Y}(x, y)=P(X \leq x, Y \leq y) =\begin{cases}
   0 & \text{ if  } x<0 \text{ or } y<0\\
   0.4 & \text{ if  } 0 \leq x<1, 0 \leq y<1\\
   0.5 & \text{ if  } x \geq 1, 0 \leq y<1\\
   0.5 & \text{ if  } 0 \leq x<1,  y \geq 1\\
   1 & \text{ if  } x \geq 1, y \geq 1,
   \end{cases}
   $$
   and the marginal cdfs 
   $$
   F_{X}(x) =F_{X,Y}(x, \infty)= \begin{cases}
   0 & \text{ if  }  x<0 \\
   0.5 & \text{ if  }  0 \leq x<1 \\
   1 & \text{ if  } x \geq 1,
   \end{cases}
   $$
   and 
   $$
   F_{Y}(y)=F_{X,Y}(\infty, y)
   = \begin{cases}
   0 & \text{ if  } y<0 \\
   0.5 & \text{ if  } 0 \leq y<1 \\
   1 & \text{ if  } y \geq 1.
   \end{cases}
   $$

:::


## Independent Random Variables

- Consider the random pair $(X,Y)$ defined on the probability space
  $(\Omega, \mathcal{F}, P)$.
- Let $\sigma(X)$ be the $\sigma$-field generated by
  $\left\{X^{-1}(S) \in \mathcal{F}: S \in \mathcal{B}\right\}$. We
  call $\sigma(X)$ be the ***$\sigma$-field generated by the r.v. $X$***.
- Similarly,  let $\sigma(Y)$ be the $\sigma$-field generated by $Y$, i.e., by
  $\left\{Y^{-1}(S) \in \mathcal{F}: S \in \mathcal{B}\right\}$.
- We say that $\sigma(X)$ and $\sigma(Y)$ are ***independent*** if the
  events $A$ and $B$ are independent for every $A \in \sigma(X)$ and
  $B \in \sigma(Y)$.
- The r.v's $X$ and $Y$ are said to be ***independent*** if
  $\sigma(X)$ and $\sigma(Y)$ are independent.  

:::{prf:remark} 
The r.v.'s $X$ $Y$ are independent if and only if any of the following
  conditions are satisfied:
1. $P_{X,Y}(A \times B)=P_{X}(A) \cdot P_{Y}(B)$ for every $A, B \in
  \mathcal{B}$.
2. $F_{X,Y}(x, y)=F_{X}(x) \cdot F_{Y}(y)$.
3. $f_{X,Y}(x, y)=f_{X}(x) \cdot f_{Y}(y)$ for continuous $(X,Y)$, or
  $p_{X,Y}(x_i, y_j)=p_{X}(x_i) \cdot p_{Y}(y_j)$ for discrete $(X,Y)$.
:::

## Conditional Distribution
- Let $(X,Y)$ be a continuous random pair. Using the previous definition of
  [conditional distribution](#rv-condist) and let the conditioned event $B=\{Y \leq
  y\}$ with $P(Y \leq y) > 0$, we have 
  $$
  F_{X}(x \mid Y \leq y) = \frac{P(X \leq x, Y \leq y)}{P(Y\leq y)} =
  \frac{F_{X,Y}(x, y)}{F_{Y}(y)}.
  $$
  Assuming that $F_{X}(x \mid Y \leq y)$ is absolutely continuous,
  the conditional pdf $f_{X}(x \mid Y \leq y) = \frac{\partial F_{X}(x
  \mid Y \leq y)}{\partial x}$ exists.

- By {prf:ref}`bayes` in the [conditional distribution](#rv-condist)
      section, we know that if $f_X(x) > 0$, then $\lim_{\Delta x
      \downarrow 0} F_{Y}(y \mid x< X \leq  x+\Delta x) = F_Y(y \mid
      X=x)$ exists and is given by
  $$
  F_{Y}(y \mid X=x) = \frac{f_{X}(x \mid Y \leq y) F_{Y}(y)}{f_{X}(x)} 
  $$
- Henceforth, we will employ the simpler notation $F_{Y\mid X}(y\mid
  x)$ to the limit $F_{Y}(y \mid X=x)$. $F_{Y\mid X}(y|x)$ is called
  the ***conditional cdf of $Y$ given $X$***.

- Since 
  $$
  \begin{aligned}
  f_{X}(x \mid Y \leq y) F_{Y}(y)
  & = \left\{ \lim _{\Delta x \downarrow 0} \frac{F_{X}(x+\Delta x | Y\leq y)
  -F_{X}(x |Y\leq y)}{\Delta x} \right\} F_Y(y)\\
  & =\lim _{\Delta x \downarrow 0} \frac{F_{X,Y}(x+\Delta x, y)-F_{X,Y}(x, y)}{\Delta x},
  \end{aligned}
  $$

  $$
  \begin{aligned} 
  & \lim _{\Delta y \downarrow 0}
  \frac{F_{Y\mid X}(y+\Delta y \mid x)-F_{Y\mid X}(y \mid x)}{\Delta y}
  \\
  &=\frac{1}{f_{X}(x)} \lim _{\Delta x \downarrow 0, \Delta y \downarrow 0}
  \frac{1}{\Delta x \Delta y}  \Big[ F_{X,Y}(x+\Delta x, y+\Delta y) \\
  & \quad\quad\quad\quad\quad\quad -F_{X,Y}(x, y+\Delta y)-F_{X,Y}(x+\Delta x,
  y)+F_{X,Y}(x, y) \Big] \\
  & = \frac{f_{X,Y}(x,y)}{f_X(x)}
  \end{aligned}
  $$
  where the last equality is due to the assumption that $(X,Y))$ is a
  continuous random pair; thus, the limit on the second line exists
  and its the joint pdf of $(X,Y)$.
- Hence, $\frac{\partial}{\partial y} F_{Y \mid X}(y \mid x)$ exists and we
  may define this derivative as the ***conditional pdf of $Y$ given
  $X$***:
  $$
  f_{Y \mid X}(y \mid x) = \frac{\partial}{\partial y} F_{Y \mid X}(y
  \mid x)=\frac{f_{X,Y}(x, y)}{f_{X}(x)}
  $$
  and thus
  $$
  f_{Y}(y)=\int_{-\infty}^{\infty} f_{X,Y}(x, y) d
  x=\int_{-\infty}^{\infty} f_{Y \mid X}(y \mid x) f_{X}(x) d x.
  $$

:::{prf:example} Bivariate Gaussian Distribution
- The continuous random pair $(X,Y)$ are said to be ***bivariate (jointly)
  Gaussian*** if its joint pdf has the form 

  $$
  \begin{aligned}
  f_{X,Y}(x, y) &= \frac{1}{2 \pi \sigma_{x} \sigma_y \sqrt{1-\rho^{2}}} \exp
  \Bigg\{-\frac{1}{2\left(1-\rho^{2}\right)} \\
  & ~~~~~~~~~~~~~~\cdot \left[\frac{(x-\mu_x)^{2}}{\sigma_{x}^{2}}-2
  \rho
  \frac{(x-\mu_x)(y-\mu_y)}{\sigma_{x}\sigma_{y}}+\frac{(y-\mu_y)^{2}}{\sigma_{y}^{2}}\right]\Bigg\}
  \end{aligned}
  $$
  where $\mu_x$, $\mu_y$, $\sigma_{x}>0, \sigma_{y}>0$, and
  $-1<\rho<1$ are parameters of the Gaussian random pair.

- Using Property 2.4, the marginal pdf of $X$ is
  $$
  \begin{aligned}
  & f_{X}(x) =\int_{-\infty}^{\infty} f_{X,Y}(x, y) d y \\
  & =\left(\frac{1}{\sqrt{2 \pi \sigma^2_{x}}}
  e^{-\frac{(x-\mu_x)^{2}}{2 \sigma_{x}^{2}}}\right) \\
 & ~~~~~~ \cdot \underbrace{\frac{1}{\sqrt{2 \pi \sigma^2_{y} (1-\rho^{2})}} \int_{-\infty}^{\infty} \exp \left(-\frac{1}{2 \sigma_{y}^{2}\left(1-\rho^{2}\right)}
 \left[y-\mu_y-\frac{\rho\sigma_{y}}{\sigma_{x}} (x-\mu_x)\right]^2
  \right) dy }_{=1}\\
  & =\frac{1}{\sqrt{2 \pi \sigma_{x}^{2}}} e^{-\frac{(x-\mu_x)^{2}}{2 \sigma_{x}^{2}}}
  \end{aligned}
  $$
  Thus,  $X$ is marginally Gaussian with mean $\mu_x$ and variance $\sigma_x^2$.

- Similarly, $Y$ is also marginally Gaussian with mean $\mu_y$ and
  variance $\sigma_y^2$. 

- Further, the conditional pdf of $Y$ give $X$ is 
  $$
  \begin{aligned}
  & f_{Y \mid X}(y \mid x) \\
  &=\frac{f_{X,Y} (x, y)}{f_X(x)} \\
  & =\frac{1}{\sqrt{2 \pi \sigma_y^{2}\left(1-\rho^{2}\right)}} 
  \exp \left(-\frac{1}{2 \sigma^{2}_y \left(1-p^{2}\right)}
  \left[y-\mu y-\frac{\rho \sigma_{y}}{\sigma_{x}}(x-\mu_x)\right]^{2}\right)
  \end{aligned}
  $$
- Notice that for any fixed $x\in\mathbb{R}$, $f_{Y \mid X}(y \mid x)$
  is a Gaussian pdf with mean $\mu_y+\frac{\rho
  \sigma_{y}}{\sigma_{x}}(x-\mu_x)$ and variance
  $\sigma_{y}^{2}\left(1-\rho^{2}\right)$. Thus, we say that $Y$ is
  ***conditional Gaussian*** given $X$.

:::
